# Public test/runtime dependencies for CI (optimization item #13).
# Pinned to the versions the test suite is known-green against.
#
# NOTE: oprim / oskill are private packages (native quant stack) not on PyPI and
# are REQUIRED — many production modules import them. CI installs them from a
# private index (PRIVATE_INDEX_URL secret); set it or the test job will fail.
pytest==9.1.1
pytest-asyncio==1.4.0
asyncpg==0.31.0
redis==8.0.1
fastapi==0.138.1
uvicorn==0.49.0
aiohttp==3.14.1
numpy==2.5.0
pandas==3.0.3
scipy==1.18.0
hmmlearn==0.3.3
ripser==0.6.15
PyWavelets==1.9.0
pydantic==2.13.4
python-dotenv==1.2.2
PyYAML==6.0.3
httpx==0.28.1
ruff==0.9.6
