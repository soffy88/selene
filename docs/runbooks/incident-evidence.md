# Incident evidence preservation

Steps: copy `orders`, `side_effects`, `write_idempotency`, logs, release SHA, artifact ids into `evidence/incidents/<date>/`. Do not rotate volumes until copied.
Owner: Soffy.
