[build-system]
requires = ["setuptools"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "cryptowatch"
version = "4.1.0"
requires-python = ">=3.11"

[project.optional-dependencies]
dev = []